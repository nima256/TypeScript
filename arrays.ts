const activeUser: string[] = ["Nima"];
activeUser.push('aajsdlk;fa');

const ageList: number[] = [55, 478, 52];
ageList[0] = 752;

// we can create array like this too
const bools: Array<boolean> = [true];
const bools2: boolean[] = [false];

// nested array 
const board: string[][] = [
    ['x', 'O', 'x'],
    ['x', 'O', 'x'],
    ['x', 'O', 'x'],
    ['x', 'O', 'x']
]